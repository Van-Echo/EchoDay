# 丸成 / EchoDay — M3 TODO 工作流验收报告

> 日期：2026-09-04  
> 状态：已通过体验验收并关闭

## 已实现

- 侧边栏与全屏当日页共享待完成/已完成分区，完成项淡化、删除线并置底。
- 支持快速新增、完整右侧编辑抽屉、完成、恢复、软删除及 Snackbar 撤销。
- 普通任务可编辑内容、所属日期、计划执行时间、计划 DDL 时间、优先级、分类、标签和备注。
- 重复规则入口保留在编辑抽屉中，规则与实例行为按计划在 M4 实现。
- 分类和标签支持创建、自选柔和颜色、选择与组合筛选。
- 支持手动、创建时间升序、创建时间降序、计划时间、优先级和综合排序。
- 排序选择写入本地设置；仅手动模式显示拖拽柄，拖拽顺序写入 Repository。
- DDL 已过且未完成的任务显示逾期状态，每分钟刷新判定。
- 支持任务右键菜单、空状态新增入口、输入框自动聚焦与 Enter 提交。

## 自动化验收

- `flutter analyze`：0 问题。
- `flutter test`：47 项全部通过。
- `flutter build windows --debug`：通过。
- Windows 启动集成测试：1 项通过。
- 集成测试后正常应用已重新构建，避免测试入口覆盖 Debug 产物。

## 启动复核

- 路径：`build/windows/x64/runner/Debug/EchoDay.exe`
- 直接启动后约 3 秒取得窗口句柄。
- 窗口标题：`丸成 EchoDay`。

## 体验验收记录

2026-09-04，用户确认 M3 使用无问题并要求继续开发，M3 正式关闭。

已验收范围：

- 在真实数据下体验编辑抽屉、完成/恢复、删除撤销和右键菜单。
- 切换六种排序，确认侧边栏与全屏页共享偏好。
- 在手动排序模式拖动多项任务并重启，确认顺序保持。
- 创建不同颜色的分类/标签并组合筛选。
- 确认浅色、深色主题下逾期和完成状态均清晰。
