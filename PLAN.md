# 丸成 / EchoDay — PC MVP 执行计划

> 计划状态：执行中
> 创建日期：2026-09-02  
> 当前阶段：M3 已关闭，M4 已实现、待体验验收
> 产品基线：[产品需求文档](docs/PRD.md)  
> 技术基线：[技术架构与开发计划](docs/DEVELOPMENT_PLAN.md)

## 1. 项目目标

完成“丸成 / EchoDay”的 Windows PC 端 MVP：以响应式月历为主体，提供日历内快速操作、当日 TODO 详情、重复任务、搜索、中国节假日与节气、本地数据库、JSON 备份恢复和浅深色主题。

第一阶段不交付 Android 和云同步，但领域模型、Repository 接口、数据标识与页面布局必须允许后续扩展。

## 2. 已确定的产品决策

- 技术框架：Flutter，Windows 先行，保留 Android 平台。
- 存储方式：本地 SQLite 数据库 + JSON 备份/恢复。
- 桌面布局：月历 + 可拖拽调宽的右侧所选日期 TODO 侧边栏；TODO 栏占可用双栏宽度 12.5%～50%，窄窗口自动单栏。
- 日历布局：横向固定 7 天，纵向为跨月连续周流；默认 5 周铺满高度，Ctrl+滚轮可调整为 5～10 周；2K/4K 纳入目标适配。
- 日期操作：单击选中并刷新侧边栏，双击将当日 TODO 放大为全屏专注模式，日期格 `+` 快速新增。
- 日期格：默认预览 6 条，可在 1～12 条之间调整，超出显示“还有 N 项”。
- 月历高度：当前可见周数均分全部可用高度；普通滚轮按周浏览，Ctrl+滚轮改变可见周数。
- 搜索：覆盖内容、备注、分类和标签，并支持状态、日期、分类、标签筛选。
- 完成行为：淡化 + 删除线 + 移动到底部；可恢复。
- 优先级：高、中、低、无。
- 分类/标签：单分类、多标签，均可自定义颜色。
- 时间字段：所属日期、计划执行时间、计划 DDL 时间。
- 逾期：未完成且当前时间超过计划 DDL 时间。
- 默认综合排序：完成状态 → 优先级 → 计划执行时间/计划 DDL 时间 → 创建时间升序。
- 重复：每天、工作日、每周、每月、自定义间隔，支持结束日期或次数。
- 中国日历：法定节假日、调休“班/休”和二十四节气；不显示完整农历。
- 外观：简约 INS 风，暖白/鼠尾草绿方向，支持浅色、深色、跟随系统。
- 删除：普通任务可撤销；重复任务选择“仅本次”或“本次及之后”。
- 备份导入：默认合并，也可覆盖；覆盖前自动安全备份。

## 3. 执行原则

1. 每次只推进当前里程碑，不提前堆叠未验证功能。
2. 领域规则先写测试，再连接数据库和界面。
3. 每个里程碑结束时必须通过静态分析、自动化测试和人工验收清单。
4. 数据迁移、备份恢复和重复任务属于高风险区域，不以手工测试代替自动化测试。
5. 不在 UI 中直接访问 Drift、文件系统或网络数据源，统一经过 Repository/Use Case。
6. 不锁死规划阶段的软件包版本；创建工程时使用当时互相兼容的稳定版本并提交锁文件。
7. 如实现与 PRD 冲突，以 PRD 为准；若需改变需求，先更新 PRD 和本计划。

## 4. 里程碑总览

| 阶段 | 产物 | 前置依赖 | 完成门槛 |
| --- | --- | --- | --- |
| P0 | 低保真原型与视觉 token | 当前计划 | 核心布局在目标窗口下可行 |
| M0 | Flutter Windows 工程骨架 | P0 | Windows 可运行，分析与测试通过 |
| M1 | 领域模型与 Drift 本地数据库 | M0 | 核心数据生命周期测试通过 |
| M2 | 自适应月历与右侧栏 | M1 | 月历交互和响应式验收通过 |
| M3 | 完整 TODO 操作与当日详情 | M2 | 普通任务端到端可用 |
| M4 | 重复任务、搜索、中国日历 | M3 | 三项复杂能力验收通过 |
| M5 | 设置、主题、备份恢复 | M4 | 离线 MVP 功能闭环 |
| M6 | Windows 发布候选 | M5 | 回归、构建和数据安全验收通过 |

## 5. 分阶段任务

### P0 — 低保真原型与视觉基线（已完成）

- [x] 在响应式交互原型中覆盖 1280×720、1920×1080、2560×1440、3840×2160 和窄窗口布局逻辑。
- [x] 验证默认 5 周均分屏幕高度与每格 TODO 实际预览上限之间的关系。
- [x] 绘制可见 5、6、8、10 周时的缩放与滚动状态。
- [x] 确定顶部工具栏、星期标题、月历和 320～360px 侧边栏的尺寸关系。
- [x] 绘制快速新增、当日详情入口和全局搜索流程；完整编辑抽屉沿用侧栏层级。
- [x] 确定浅色/深色基础色、文字层级、圆角、间距和任务行高度 token 初稿。
- [x] 根据第二轮反馈撤销横向多月方案，改为固定 7 列的纵向连续周流。
- [x] 增加默认 5 周、Ctrl+滚轮 5～10 周缩放的第三轮原型。
- [x] 撤销分辨率固定高度，改为读取实时窗口内容高度并让日期网格占满全部剩余空间。
- [x] 将每月 1 日改为整格中文月份数字水印，仅显示“一”“二”“十一”等，日期 `1` 保持独立。
- [x] 将双击日期调整为全屏当日 TODO，并保留单击选日与右侧栏。
- [x] 增加月历/TODO 栏拖拽分隔线原型，覆盖 12.5%～50% 限制、键盘调宽及窗口缩放保持比例。
- [x] 形成原型评审记录；把确认后的变化回写 PRD。

验收门槛：全部目标窗口尺寸无关键内容重叠；默认 5 周精确铺满；5～10 周均能顺畅选日、滚动、缩放和回到今天。

### M0 — 工程与质量骨架（已完成）

- [x] 检查 Flutter stable、Dart、Visual Studio Windows 桌面工具链。
- [x] 创建仅启用 Windows 与 Android 的 Flutter 工程。
- [x] 配置项目名称、应用标识、静态分析和格式化规则。
- [x] 建立 feature-first 目录、环境配置、错误边界和日志抽象。
- [x] 接入 Riverpod、go_router、主题骨架和中英文本地化骨架。
- [x] 建立单元、widget 和集成测试入口。
- [x] 加入基础 CI 和一键本地质量检查脚本。

完成记录：Flutter 3.47.2 / Dart 3.13.2、Visual Studio 2022 17.14.39 与 Windows SDK 10.0.22621.0 已通过 doctor；`flutter analyze` 无问题，4 项普通测试与 1 项 Windows 启动集成测试通过，Debug `EchoDay.exe` 构建成功。详见 `docs/M0_SETUP_REPORT.md`。

验收门槛：`flutter run -d windows` 可启动；`flutter analyze` 与 `flutter test` 通过；空壳导航可进入月历、当日详情、搜索、设置和关于页。

### M1 — 领域模型与本地数据库（已完成）

- [x] 定义 Todo、Category、Tag、RecurrenceSeries、HolidayYear、Settings 实体和值对象。
- [x] 定义 TodoRepository、HolidayRepository、BackupRepository 等接口。
- [x] 实现 UUID、UTC 时间、所属本地日期、修订号和软删除约定。
- [x] 建立 Drift 表、索引、外键、schemaVersion 和首个迁移。
- [x] 实现任务 CRUD、完成/恢复、撤销删除、分类和标签关系。
- [x] 实现按日期响应式查询和手动顺序持久化。
- [x] 实现纯 Dart 排序引擎与逾期判断。
- [x] 为数据库和排序规则补齐单元/集成测试。

完成记录：Drift schema v1、领域模型、Repository 接口及本地实现已完成；`flutter analyze` 无问题，26 项普通测试通过，包括真实 SQLite 文件关闭重开一致性、外键事务回滚、生命周期、搜索、排序与逾期边界。详见 `docs/M1_DATA_REPORT.md`。

验收门槛：无 UI 条件下可完成新增、编辑、完成、恢复、删除、撤销和查询；重启数据库后数据一致；排序与逾期边界测试通过。

### M2 — 月历工作台（已完成）

- [x] 实现周一开头、跨月份不中断的连续周日期网格。
- [x] 实现上月、下月、年月标题和回到今天悬浮按钮。
- [x] 实现日期单击选中、双击进入全屏当日 TODO，并支持返回状态恢复。
- [x] 实现日期格 TODO 摘要、`+` 快速新增和“还有 N 项”。
- [x] 实现固定 7 列、5～10 周、行高均分与实际预览条数的纯计算布局模型。
- [x] 使用 `Expanded + LayoutBuilder` 读取实时剩余高度，禁止硬编码分辨率对应的日历高度。
- [x] 实现跨月连续周流、粘性星期标题、普通滚轮浏览和 Ctrl+滚轮缩放。
- [x] 实现调整窗口或设置后保持选中日期可见。
- [x] 实现双栏拖拽分隔线：悬停左右拉动光标、TODO 栏 12.5%～50%、键盘调宽与双击恢复默认。
- [x] 持久化 TODO 栏宽度比例；窗口缩放保持比例，窄窗口隐藏后恢复双栏时还原。
- [x] 实现窄窗口单栏降级。
- [x] 覆盖 1280×720、1920×1080、2560×1440、3840×2160 和窄窗口的 widget 测试，并抽测 100%～200% 显示缩放。

实现记录：连续周月历、日期摘要、快速新增、全屏当日 TODO、5～10 周缩放、普通滚轮周流、可调宽侧栏和设置持久化均已接入真实 Repository；44 项普通测试与 1 项 Windows 启动集成测试通过。详见 `docs/M2_CALENDAR_REPORT.md`。

验收记录：2026-09-04 用户确认体验无问题，M2 正式关闭。

验收门槛：默认 5 周精确铺满；Ctrl+滚轮在 5～10 周间缩放；普通滚轮按周浏览；预览条数与窗口变化无溢出；TODO 栏只能在 12.5%～50% 间调整并在重启后保持；月切换和今天定位准确。

### M3 — TODO 完整交互

- [x] 实现侧边栏和当日详情的未完成/已完成分区。
- [x] 实现内容快速新增与 Enter 提交。
- [x] 实现完整编辑抽屉及普通任务字段；重复规则按 M4 边界展示为暂不可用。
- [x] 实现完成、恢复、删除与撤销反馈。
- [x] 实现分类、标签的创建、选取、颜色和筛选。
- [x] 实现手动拖拽、创建时间、计划时间、优先级和综合排序。
- [x] 只在手动模式启用拖拽，并记住上次排序选项。
- [x] 实现逾期视觉提示和跨日刷新。
- [x] 补齐键盘焦点、右键菜单和空状态。

实现记录：侧边栏与全屏当日页已共享完整普通任务生命周期、右侧编辑抽屉、分类标签、筛选、六种排序及 Sliver 手动拖拽；47 项普通测试与 1 项 Windows 启动集成测试通过。详见 `docs/M3_TODO_WORKFLOW_REPORT.md`。

验收记录：2026-09-04，用户确认使用无问题并要求继续开发，M3 正式关闭。

验收门槛：从日期格、侧边栏和当日详情均可完成普通 TODO 生命周期；完成项正确置底；全部排序与拖拽持久化通过验收。

### M4 — 重复、搜索与中国日历

#### M4.1 重复任务

- [x] 实现规则展开器：每天、工作日、每周、每月、自定义间隔。
- [x] 实现结束日期和重复次数。
- [x] 实现实例例外，使单次完成、编辑和删除互不影响其他发生项。
- [x] 实现“仅本次”和“本次及之后”，后者通过规则截断与新版本处理。
- [x] 实现节假日已知/未知情况下的工作日判断与提示。
- [x] 实现当日未完成任务批量顺延；同步移动执行时间/DDL，重复任务仅作用于当前发生项。

#### M4.2 全局搜索

- [x] 实现内容、备注、分类和标签搜索。
- [x] 实现完成状态、日期范围、分类和标签筛选。
- [x] 展示日期、状态、优先级、执行时间和 DDL。
- [x] 点击结果后定位日期、打开对应任务并给予视觉聚焦。
- [x] 根据当前本地数据规模沿用普通查询，并保留 Repository 分页边界；暂不启用 FTS5。

#### M4.3 中国日历

- [x] 定义年度节假日 JSON schema、字段校验与 SHA-256 完整性校验器。
- [x] 建立 Bundled、Cached、Remote 三层数据源；Remote 接入国务院政策文件库与中国政府网正文解析。
- [x] 内置官方已公布的 2025、2026 年节假日和调休数据并记录来源。
- [x] 实现按年份检查更新、数据库优先、官网抓取入库、失败回退、覆盖年份和数据版本展示。
- [x] 实现基于太阳黄经的二十四节气本地计算与边界测试。
- [x] 在日期格展示节日、节气与“班/休”，控制信息密度。

实现记录：重复任务采用规则与虚拟发生项展开，发生项变更时才实体化，并支持当日未完成项一键顺延；全局搜索支持四类文本与组合筛选并可直达编辑器；中国日历具备内置/缓存/官网三层数据、严格校验、缺失年份提示和设置页手动更新。详见 `docs/M4_COMPLEX_FEATURES_REPORT.md`。

验收状态：实现、自动化验收与用户体验确认均已完成；M4 于 2026-09-05 关闭。

验收门槛：重复实例可独立完成；搜索四类文本可命中并定位；离线显示内置日历数据；无目标年份数据时不猜测调休且提示明确。

### M5 — 设置、主题与数据安全

状态：进行中（2026-09-05 启动）。

- [x] 实现浅色、深色、跟随系统和主色设置。
- [x] 实现日期格预览 1～12 条设置与默认排序设置。
- [x] 实现版本化 JSON 导出和标准文件名。
- [x] 实现导入预检、合并导入和 UUID 去重。
- [x] 实现覆盖恢复、二次确认和覆盖前自动安全备份。
- [x] 所有导入操作在事务中执行，失败保持原数据库不变。
- [x] 实现设置、节假日和关于页面。
- [x] 完成浅色/深色视觉修整、可访问性与品牌信息。

实现记录：新增 JSON v1 备份仓库与设置页文件交互；备份覆盖全部用户实体和设置并排除节假日缓存。导入先限制文件大小并校验版本、字段、UTC 时间、ID 唯一性、重复规则和引用关系；合并按稳定 ID/关系键去重，覆盖恢复需二次确认并先写入应用支持目录的安全备份。主题模式、主色、预览条数与默认排序均通过 SettingsRepository 持久化。详见 `docs/M5_SETTINGS_BACKUP_REPORT.md`。

验收状态：实现、自动化验收与用户体验确认均已完成；M5 于 2026-09-05 关闭。

验收门槛：导出 → 清空 → 恢复后数据一致；错误备份不修改数据；三种主题和设置重启后保持；节假日缓存不进入用户备份。

### M6 — Windows 发布候选

状态：进行中（2026-09-05 启动）。

- [x] 运行全部静态分析、单元、集成和 widget 测试；当前无 golden 基线，记录为不适用。
- [x] 验证数据库从 schema v1 无损升级至 v2。
- [x] 验证 10,000 条任务下的搜索性能和活动日期查询索引；月份/周流计算继续由纯函数测试覆盖。
- [ ] 在 Windows 10/11 x64 的目标环境完成安装、启动和卸载测试（Windows 11 已通过；Windows 10 实机待外部冒烟）。
- [x] 确认便携版删除程序目录不会意外删除用户数据库。
- [x] 生成 Release 构建、版本说明、已知限制和备份说明。
- [x] 形成 Android 阶段差异清单，但不在本阶段实现。

实现记录：数据库升级至 v2 并增加活动任务日期/完成状态复合索引，Drift schema verifier 已验证 v1→v2 结构与真实任务/设置数据完整性。质量流水线 92 项通过、1 项真实官网测试默认跳过；Windows 原生集成启动测试 1 项通过。RC1 便携 ZIP 已在 Windows 11 x64 解压启动，并验证删除程序目录后 `Documents/echoday.sqlite` 保留。包内含 AGPL、备份指南、发布说明和应用本地 Visual C++ 运行库，且自动校验不含数据库或备份 JSON，另提供 SHA-256。详见 `docs/M6_WINDOWS_RC_REPORT.md`。

验收状态：RC1 已完成本机自动化和 Windows 11 体验前验证；等待用户体验确认及 Windows 10 x64 外部冒烟后关闭 M6。

验收门槛：PRD 第 13 节全部通过；无阻断级缺陷；Release 包可供第一轮试用。

## 6. 每个里程碑的固定检查

完成任何里程碑前必须依次检查：

- [ ] 需求：实现与 PRD 一致，新增决策已记录。
- [ ] 数据：迁移、失败回滚、软删除和时间字段无破坏。
- [ ] 质量：格式化、静态分析和全部相关测试通过。
- [ ] UI：键盘、鼠标、浅色、深色和目标窗口尺寸已验证。
- [ ] 文档：更新本文件勾选状态、已知限制和下一阶段入口。

## 7. 暂不实施清单

- 云账号、云同步和服务端。
- Android 界面适配与发布。
- 系统通知、闹钟和后台提醒。
- 第三方日历导入或同步。
- 多用户、共享列表和协作。
- 完整农历展示。

这些功能不得在 MVP 开发中顺带加入；只能保留接口或数据兼容性。

## 8. 进度维护规则

- 任务开始时将对应阶段标记为“进行中”，仅在验收门槛通过后勾选完成。
- 若一个任务被拆分，新增子项但保留原验收目标。
- 失败实验或被放弃方案记入阶段备注，不把未验证代码留在主路径。
- 每次暂停开发时，在下方更新“当前执行点”，保证下次可直接续接。

## 9. 当前执行点

- 当前：M6 RC1 已生成；schema v1→v2、10,000 条任务、全量测试、Windows 11 原生启动、便携安装/删除和用户数据保留均已通过。
- 下一项：由用户体验 RC1，并在可用的 Windows 10 x64 环境完成一次启动冒烟；随后关闭 M6。
- 开发状态：P0、M0、M1、M2、M3、M4、M5 已完成；M6 RC1 已实现、待体验与 Windows 10 外部验收。
- 阻塞项：无。
